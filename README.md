#PalindroneCheckerApp
Start the application.
Display welcome message.
Ask the user to enter a string or number.
Reverse the input.
Compare the original input with the reversed input.
If both are equal → display "It is a Palindrome".
Otherwise → display "It is Not a Palindrome".
End the program.